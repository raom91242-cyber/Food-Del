{
  "name": "food-del",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "npm --prefix frontend install && npm --prefix admin install && npm --prefix frontend run build && npm --prefix admin run build && node scripts/prepare-vercel-output.js"
  }
}
